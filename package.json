{
  "name": "es6-express",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "export NODE_ENV=development && node_modules/.bin/webpack --env development --progress --colors --watch",
    "build": "export NODE_ENV=production && node_modules/.bin/webpack -p --env production --progress --colors",
    "start": "node dist/server.bundle.min.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon-webpack-plugin": "^0.1.2",
    "path": "^0.12.7",
    "webpack": "fulls1z3/webpack"
  },
  "dependencies": {
    "express": "^4.15.4"
  }
}
